package com.cafeteria.destination.top

import androidx.compose.material.icons.Icons
import androidx.compose.material.icons.outlined.Add
import androidx.compose.material.icons.outlined.AutoStories
import androidx.compose.material.icons.outlined.BrowseGallery
import androidx.compose.material.icons.outlined.Home
import androidx.compose.material.icons.outlined.Settings
import androidx.compose.ui.graphics.vector.ImageVector
import com.cafeteria.R


/*
sealed class TopNavMenuItem(
    val route: String,
    val title: Int,
    val icon: ImageVector,
) {
    data object Home: TopNavMenuItem(
        route = R.string.home.toString(),
        title = R.string.home_title,
        icon = Icons.Outlined.Home,
    )
    data object Reservation: TopNavMenuItem(
        route = R.string.reservation.toString(),
        title = R.string.reservation_title,
        icon = Icons.Outlined.BrowseGallery,
    )
    data object Board: TopNavMenuItem(
        route = R.string.board.toString(),
        title = R.string.board_title,
        icon = Icons.Outlined.AutoStories,
    )
    data object Settings: TopNavMenuItem(
        route = R.string.settings.toString(),
        title = R.string.settings_title,
        icon = Icons.Outlined.Settings,
    )
    data object Insert: TopNavMenuItem(
        route = R.string.insert.toString(),
        title = R.string.insert_title,
        icon = Icons.Outlined.Add
    )
}
*/
