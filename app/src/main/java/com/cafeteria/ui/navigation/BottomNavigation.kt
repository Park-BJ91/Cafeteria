package com.cafeteria.ui.navigation


/*
@Composable
fun BottomNavigation(
    navi: (String) -> Unit,
    navViewModel: BottomNavViewModel = viewModel(),
    modifier: Modifier
) {

    val naviState = navViewModel.bottomNavState.collectAsState()


    Box(
        modifier = modifier
    ) {
        Row(
            modifier = Modifier
                .fillMaxWidth()
                .height(80.dp)
                .padding(15.dp),
            horizontalArrangement = Arrangement.SpaceBetween,
            verticalAlignment = Alignment.CenterVertically
        ) {

            HomeNav(navi = navi, viewModel = navViewModel, naviState = naviState)
            ReservationNav(navi = navi, viewModel = navViewModel, naviState = naviState)
            BoardNav(navi = navi)
            SettingsNav(navi = navi)
        }
    }

}
*/





